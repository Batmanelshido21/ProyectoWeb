<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtdmybatis-3-mapper.dtd">

<mapper namespace="Bitacora">
    
<insert id="registrarBitacora" parameterType="pojos.Bitacora">
        INSERT INTO bitacora (idBitacora,fecha,archivo,Docente_clave)
        VALUES(#{idBitacora},#{fecha},#{archivo},#{Docente_clave})        
 </insert>
  
</mapper>